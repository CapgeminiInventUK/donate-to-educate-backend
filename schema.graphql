type School {
  urn: ID!
  name: String!
  localAuthority: String!
  postcode: String!
  registered: Boolean!
}

type Query {
  getSchoolByName(name: String!): School!
  getSchoolsByLa: [School!]!
}

schema {
  query: Query
}
